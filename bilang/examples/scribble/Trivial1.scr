explicit global protocol Trivial1(role Server, role A) {
    connect Server to A;
    Broadcast() from Server to ;
    Withdraw() from A to Server;
}


explicit local protocol Trivial1_A(role Server, role A) {
    connect Server;
    Withdraw() to Server;
}
